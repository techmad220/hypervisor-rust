[package]
name = "driver-self-protection"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
winapi = { version = "0.3", features = ["ntdef", "winnt", "wdm"] }
ntapi = "0.4"